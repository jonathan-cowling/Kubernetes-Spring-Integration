apiVersion: batch/v1
kind: Job
metadata:
  name: spring
  labels:
    app: spring-job
spec:
  template:
    spec:
      containers:
      - name: job
        image: streamjobexample:local
      restartPolicy: Never
  backoffLimit: 1